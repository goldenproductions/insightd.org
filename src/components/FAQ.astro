---
const faqs = [
  {
    question: 'Is insightd free?',
    answer: 'Yes. insightd is MIT licensed and free forever. No paid tiers, no cloud lock-in.',
  },
  {
    question: 'How many servers can I monitor?',
    answer: 'Unlimited. Deploy an agent on each server and they all report to a single hub. The lightweight MQTT protocol keeps overhead minimal even at scale.',
  },
  {
    question: 'Does it phone home?',
    answer: 'No. Zero telemetry, no cloud dependency, no external API calls. Your monitoring data stays on your hardware.',
  },
  {
    question: 'What are the system requirements?',
    answer: 'Minimal. The hub uses ~28 MB of RAM and each agent uses ~20 MB. Storage is SQLite — no external database needed. Runs on anything that runs Docker, including a Raspberry Pi.',
  },
  {
    question: 'Does it support Kubernetes?',
    answer: 'Yes. Deploy the agent as a DaemonSet on k8s or k3s — one pod per node. Each pod reports its containers and node-level metrics. Host metrics come from the kubelet, not /proc, so they are accurate even inside a container.',
  },
  {
    question: 'What notification channels are supported?',
    answer: 'Slack, Discord, Telegram, ntfy, and generic webhooks. Plus HTML email alerts and weekly digest summaries.',
  },
  {
    question: 'How does the diagnosis engine work?',
    answer: 'When a container is unhealthy, insightd runs seven signal detectors against metrics, robust baselines (MAD z-scores), restart history, host state, Drain-mined log templates, and service topology (Personalized PageRank). The unified diagnoser fuses the results, ranks evidence, and tells you what went wrong and what to try. Based on published research.',
  },
  {
    question: 'Is there a demo?',
    answer: 'Not yet — insightd is designed to run on your own hardware. The setup wizard gets you running in under 5 minutes.',
  },
];
---

<section id="faq" class="py-16 sm:py-20 lg:py-20">
  <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:max-w-4xl">
    <div class="text-center">
      <h2 class="text-2xl font-bold text-fg sm:text-3xl">Frequently asked questions</h2>
    </div>

    <div class="mt-10 space-y-3">
      {faqs.map(faq => (
        <details class="group rounded-xl border border-border bg-surface">
          <summary class="flex cursor-pointer items-center justify-between px-5 py-4 text-sm font-medium text-fg">
            {faq.question}
            <svg class="h-4 w-4 shrink-0 text-muted transition-transform group-open:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="border-t border-border px-5 py-4">
            <p class="text-sm text-muted leading-relaxed">{faq.answer}</p>
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
